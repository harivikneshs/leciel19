<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.barebrains.leciel19">

    <application
        android:name=".gyanith19"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="Leciel 19"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.Noactionbar">
        <activity android:name=".RegisterActivity"></activity>
        <activity android:name=".register" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".MainActivity"
            android:label="Leciel 19" />
        <activity android:name=".RegisterWebView" />
        <activity android:name=".event_categories" />
        <activity android:name=".event_details" />
        <activity android:name=".profile" />
        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".about" />

        <receiver android:name=".notreceiver">
            <intent-filter>
                <action android:name="gyanith.notify" />
            </intent-filter>
        </receiver>
    </application>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>